%h2.main New project
= error_messages_for :project
.cols.cols2v1
  .col1
    .box.profileForm.formBox
      - form_for(:project, :url => collection_url, :html => { :multipart => true }) do |f|
        = render :partial => "form", :locals => { :f => f }
        %p= submit_tag "Create", :class => 'submit'

= link_to 'Back', collection_url