%h2.main Editing project
= error_messages_for :project
.cols.cols2v1
  .col1
    .box.profileForm.formBox
      - form_for(:project, :url => object_url, :html => { :method => :put, :multipart => true }) do |f|
        = render :partial => "form", :locals => { :f => f }
        %p= submit_tag "Update", :class => 'submit'
  
= link_to 'Show', object_url
|
= link_to 'Back', collection_url